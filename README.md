# weather-app-interface
A clean, responsive weather app UI built with HTML and CSS. This project demonstrates strong front-end styling (Flexbox, gradients) and the use of inline SVGs for a modern, scalable interface.
